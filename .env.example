# Client-side (available in Expo via process.env)
EXPO_PUBLIC_CONVEX_URL=
EXPO_PUBLIC_WORKOS_CLIENT_ID=

# Server-side secrets go into Convex environment variables (not here)
# Use: pnpm dlx convex env set KEY value
